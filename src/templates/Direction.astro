---
// Shared template for the two business directions.
// Page shape: lead → summary cards → typed content blocks → CTA.
// A third direction = 3 content files + one line in sections.json. No code change.
import ContentBlock from '../components/ContentBlock.astro';
import SummaryCards from '../components/SummaryCards.astro';
import { pagePath } from '../lib/content.js';

const { locale, content } = Astro.props;
const contactsPath = pagePath(locale, 'contacts');
---
<section class="section section-lead">
  <div class="container">
    <p class="eyebrow">{content.eyebrow}</p>
    <h1>{content.hero.title}</h1>
    <div class="section-intro">
      <p class="lead">{content.hero.lead}</p>
      {content.hero.ctaLabel && (
        <a class="btn btn-ghost" href={`#${content.cardsAnchor}`}>{content.hero.ctaLabel}</a>
      )}
    </div>
  </div>
</section>

<SummaryCards
  locale={locale}
  anchor={content.cardsAnchor}
  title={content.cardsTitle}
  cards={content.cards}
/>

{content.blocks.map((block) => <ContentBlock block={block} />)}

<section class="section">
  <div class="container">
    <h2>{content.cta.title}</h2>
    <div class="section-intro">
      <p>{content.cta.text}</p>
    </div>
    <a class="btn btn-primary" href={contactsPath}>{content.cta.button}</a>
  </div>
</section>
