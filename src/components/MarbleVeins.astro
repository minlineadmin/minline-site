---
// Decorative marble veining echoing the logo's stone.
// The artwork is a generated SVG slab under /img (see scripts/gen_veins.py):
// a crack network of tapered, branching veins with a warm haze over a mottled
// stone field, spread across the whole surface with a dominant flow direction
// and a few quiet zones. Kept as an external file rather than inline markup —
// it would otherwise be repeated in all 18 pages.
// Inert to pointer events and behind every content layer.
---
<div class="veins" aria-hidden="true"></div>
