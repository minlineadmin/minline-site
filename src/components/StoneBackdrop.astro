---
// The stone backdrop: the client's own marble photograph — the same texture the
// logo was built on — fixed behind the whole site.
//
// This replaces a generated SVG vein network. No procedural drawing can match a
// specific photograph, and "exactly like the logo" was the requirement.
//
// The reading band that keeps text legible over it lives in global.css; see the
// note there before changing --stone-veil or --stone-scrim.
---
<div class="stone" aria-hidden="true"></div>
