---
// Emits /en/404.html. Cloudflare serves the nearest 404.html up the tree,
// so a broken address under /en gets this locale's page.
import NotFound from '../../templates/NotFound.astro';
---
<NotFound locale="en" />
